import React,{useState} from 'react';
import {Modal,View,Text,ScrollView,Pressable} from 'react-native';
import {exercises} from '../lib/model';
import {Button,Field,C,s} from './ui';
export function ExercisePicker({value,onChange,allowAll=false}:{value:string;onChange:(id:string)=>void;allowAll?:boolean}){const [open,setOpen]=useState(false);const [search,setSearch]=useState('');const name=exercises.find(e=>e.id===value)?.name||(allowAll?'Todos los ejercicios':'Elegir ejercicio');const options=exercises.filter(e=>(e.name+' '+e.group).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().includes(search.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase()));return <><Button title={name+'  ▾'} secondary onPress={()=>{setSearch('');setOpen(true)}}/><Modal visible={open} transparent animationType="fade" onRequestClose={()=>setOpen(false)}><View style={{flex:1,backgroundColor:'#000B',justifyContent:'center',padding:20}}><View style={[s.card,{width:'100%',maxWidth:520,maxHeight:'85%',alignSelf:'center'}]}><View style={s.between}><Text style={s.h2}>Elige un ejercicio</Text><Button small title="Cerrar" secondary onPress={()=>setOpen(false)}/></View><Field label="Buscar ejercicio o grupo muscular" value={search} onChangeText={setSearch}/><ScrollView keyboardShouldPersistTaps="handled">{allowAll&&<Button title="Todos los ejercicios" secondary onPress={()=>{onChange('all');setOpen(false)}}/>}{options.map(e=><Pressable accessibilityRole="button" key={e.id} onPress={()=>{onChange(e.id);setOpen(false)}} style={{paddingVertical:16,borderBottomWidth:1,borderBottomColor:C.edge}}><Text style={s.text}>{e.name}</Text><Text style={s.muted}>{e.group}</Text></Pressable>)}{!options.length&&<Text style={s.muted}>No hay ejercicios con ese nombre.</Text>}</ScrollView></View></View></Modal></>}
